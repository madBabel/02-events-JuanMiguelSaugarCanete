name: 02 - Eventos
on: 
    push:
    workflow_dispatch:
    schedule:
        - cron: "3 * * * *"
        
jobs:
    ShowTrigger:
        runs-on: ubuntu-latest
        steps:
            - name: ShowTrigger
              run: echo "${{ github.event_name }}"